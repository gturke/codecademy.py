##Create a function to determine whether a number is prime:

def is_prime(x):
    if x<=1:
        return False
    elif x==2:
        return True
    else:
        z = x-1
        while z > 1:
            if x % z == 0:
                return False
            z = z-1
        else:
            return True
